<% Houston.config.project_colors.each do |name, value| %>
  $<%= name %>: <%= value.rgb %>;
<% end %>

.roadmap-band {
  <% Houston.config.project_colors.each do |name, value| %>
    &.<%= name %> {
      .roadmap-milestone::before { background: <%= value.rgb %>; }
    }
  <% end %>
}

body.dashboard .roadmap-band {
  <% Houston.config.project_colors.each do |name, value| %>
    &.<%= name %> {
      .roadmap-milestone { border-color: <%= value.rgb %>; }
    }
  <% end %>
}
